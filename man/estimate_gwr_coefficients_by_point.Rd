% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_gwr_coefficients.R
\name{estimate_gwr_coefficients_by_point}
\alias{estimate_gwr_coefficients_by_point}
\title{Geographically weighted local regression coefficients at points}
\usage{
estimate_gwr_coefficients_by_point(
  data,
  unit,
  formula,
  coords = c("longitude", "latitude"),
  predict_data = NULL,
  distance_metric = "Euclidean",
  kernel = "bisquare",
  adaptive = TRUE,
  bw = NULL,
  bw_response = NULL,
  bw_approach = "CV",
  terms = NULL
)
}
\arguments{
\item{data}{Source units (\code{data.table}/data frame) with the model variables
and the coordinate columns in \code{coords}.}

\item{unit}{Character; the unit-id column (present in source and target).}

\item{formula}{A model formula, e.g. \code{y ~ x} or \code{y ~ x1 + x2}.}

\item{coords}{Length-2 character vector naming longitude/latitude columns.
Default \code{c("longitude", "latitude")}.}

\item{predict_data}{Optional TARGET units with \code{unit} and \code{coords}. Defaults
to \code{data} (targets = sources).}

\item{distance_metric}{One of \code{gw_distance_metric_names()}. Default
\code{"Euclidean"}.}

\item{kernel}{GW kernel. Default \code{"bisquare"}.}

\item{adaptive}{Logical; adaptive (kNN) bandwidth if \code{TRUE}. Default \code{TRUE}.}

\item{bw}{Optional pre-computed bandwidth. If \code{NULL}, selected once via
\code{GWmodel::bw.gwr(bw_response ~ 1)} on the source set.}

\item{bw_response}{Column for bandwidth selection when \code{bw} is \code{NULL}.
Default: the response of \code{formula}.}

\item{bw_approach}{Bandwidth criterion. Default \code{"CV"}.}

\item{terms}{Optional character vector restricting the returned terms
(e.g. \code{c("avail00")}). Default: all terms including the intercept.}
}
\value{
A \code{data.table} with one row per (target unit x term): \code{term},
\code{unit_id}, \code{unit_level}, \code{est}, \code{se}, \code{tv}, \code{pv}, \code{model_estimator}. The
selected bandwidth is attached as \code{attr(., "bandwidth")}.
}
\description{
For each target unit, fits \code{lm(formula, weights = w_i)} where \code{w_i} are
\code{GWmodel::gw.weight()} weights over the source units, and returns the full
per-unit coefficient table. Accepts an arbitrary (multi-covariate) formula and
returns exactly what \code{stats::lm()} reports.
}
\seealso{
Other Geographically weighted regression: 
\code{\link{estimate_gwfe_coefficients_by_point}()},
\code{\link{estimate_gwfe_coefficients_by_polygon}()},
\code{\link{estimate_gwr_by_point}()},
\code{\link{estimate_gwr_by_polygon}()},
\code{\link{estimate_gwr_coefficients_by_polygon}()}
}
\concept{Geographically weighted regression}
