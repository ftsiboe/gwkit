% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gw_consensus.R
\name{gw_optimal_class_by_polygon}
\alias{gw_optimal_class_by_polygon}
\title{Optimal class for polygon units}
\usage{
gw_optimal_class_by_polygon(
  class_dt,
  unit_col,
  polygons,
  class_col = "trend_class_05",
  poly_id = unit_col,
  include_self = TRUE,
  max_order = 10L,
  class_levels = NULL,
  verbose = FALSE
)
}
\arguments{
\item{class_dt}{A \code{data.table}/data frame stacked over settings, containing
the unit id (\code{unit_col}) and the categorical class (\code{class_col}).}

\item{unit_col}{Name of the spatial-unit identifier column (e.g.
\code{"polygon_fips"}).}

\item{polygons}{A \code{terra} \code{SpatVector} or \code{sf} polygon layer whose id field
(\code{poly_id}) matches \code{unit_col}.}

\item{class_col}{Name of the categorical class column. Default
\code{"trend_class_05"}.}

\item{poly_id}{Name of the id field in \code{polygons}. Default: value of
\code{unit_col}.}

\item{include_self, max_order, class_levels, verbose}{See
\code{gw_optimal_class_by_point()}.}
}
\value{
A \code{data.table} with one row per unit (see
\code{gw_optimal_class_by_point()}).
}
\description{
As \code{gw_optimal_class_by_point()}, but the spatial units are polygons: the
Queen neighbours are shared-boundary contiguities of a supplied geometry
(matching \code{estimate_gwss_by_polygon()}, keyed by e.g. \code{polygon_fips}), and
centroids for the output come from the same geometry. Accepts a \code{terra}
\code{SpatVector} or an \code{sf} object.
}
\seealso{
Other Optimal class selection: 
\code{\link{gw_optimal_class_by_point}()}
}
\concept{Optimal class selection}
