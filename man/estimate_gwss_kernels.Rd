% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_gwss.R
\name{estimate_gwss_kernels}
\alias{estimate_gwss_kernels}
\title{Estimate GWSS across kernels for one distance metric (point mode)}
\usage{
estimate_gwss_kernels(
  data,
  variable_list,
  coords = c("longitude", "latitude"),
  predict = NULL,
  distance_metric = "Euclidean",
  kernel = c("gaussian", "exponential", "bisquare", "boxcar", "tricube"),
  adaptive = TRUE,
  approach = "CV",
  target_crs = 5070,
  draw_rate = 0.5,
  feasible_radius = 100,
  identifiers = NULL,
  bandwidth = c("shared", "per_kernel"),
  bw = NULL
)
}
\arguments{
\item{data}{A \code{data.table}/data frame with \code{variable_list} and either the
\code{coords} columns (point mode) or the \code{id_col} join key (polygon mode).}

\item{variable_list}{Character vector of numeric variable column(s) to
summarize (passed to \code{GWmodel::gwss(vars = )}). The bandwidth uses the first.}

\item{coords}{Length-2 character vector naming longitude/latitude columns
(point mode). Default \code{c("longitude", "latitude")}.}

\item{predict}{Optional prediction locations (point mode): a data frame with
the \code{coords} columns (and any \code{identifiers}). Defaults to the observed points.}

\item{distance_metric}{One of \code{gw_distance_metric_names()}. Default
\code{"Euclidean"}.}

\item{kernel}{Character vector of kernels to evaluate (any of \code{"gaussian"},
\code{"exponential"}, \code{"bisquare"}, \code{"boxcar"}, \code{"tricube"}).}

\item{adaptive}{Logical; adaptive (kNN) bandwidth if \code{TRUE}. Default \code{TRUE}.}

\item{approach}{Bandwidth criterion, one of \code{"CV"}, \code{"AIC"}, \code{"AICc"}.
Default \code{"CV"}.}

\item{target_crs}{Integer EPSG code (meters) for projection (polygon geometry
and the point-mode radius screen). Default \code{5070} (NAD83 / CONUS Albers).}

\item{draw_rate}{Fraction of observed units used for bandwidth selection.
Default \code{0.5}.}

\item{feasible_radius}{Radius in miles for pre-screening prediction points
(point mode only). Default \code{100}.}

\item{identifiers}{Optional character vector of columns in \code{predict} (point
mode) to carry through to the output.}

\item{bandwidth}{Bandwidth strategy across the supplied kernels: \code{"shared"}
(default) selects one bandwidth (from the first kernel) and reuses it for all
kernels - one \code{bw.gwr()} call instead of \code{length(kernel)}; \code{"per_kernel"}
selects a bandwidth per kernel (matches looping \code{estimate_gwss()} exactly).}

\item{bw}{Optional numeric bandwidth override. When supplied it is used for
every kernel and no bandwidth selection is performed (overrides \code{bandwidth}).}
}
\value{
A \code{data.table} stacked over \code{kernel} (one block of prediction rows per
kernel), with the GWSS statistics, coordinates, \code{identifiers}, and a \code{kernel}
column. \code{distance_params} is attached as an attribute; when a single shared
bandwidth is used it is attached as \code{bandwidth}.
}
\description{
Point-mode companion to \code{estimate_gwss()} that evaluates several kernels for a
single \code{distance_metric} while computing the (kernel-independent) distance
matrices and radius screen only \strong{once}, reusing them across kernels. This is
the per-metric building block for spec ensembles: much cheaper than calling
\code{estimate_gwss()} separately for each kernel.
}
\seealso{
Other Geographically weighted summaries: 
\code{\link{estimate_gwlag}()},
\code{\link{estimate_gwss}()},
\code{\link{gw_select_bandwidth}()}
}
\concept{Geographically weighted summaries}
