% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_gwss.R
\name{estimate_gwss_by_county}
\alias{estimate_gwss_by_county}
\title{Estimate geographically weighted summary statistics (GWSS) for counties}
\usage{
estimate_gwss_by_county(
  data,
  fip_col,
  variable_list,
  distance_metric = "Euclidean",
  kernel = "gaussian",
  target_crs = 5070,
  draw_rate = 0.5,
  approach = "CV",
  adaptive = TRUE,
  state_fips_limits = NULL
)
}
\arguments{
\item{data}{A \code{data.frame}/\code{data.table} with at least \code{fip_col} and all
columns listed in \code{variable_list}.}

\item{fip_col}{Character. Name of the county ID column in \code{data}; copied to
\code{"county_fips"} for joining with the county map.}

\item{variable_list}{Character vector. Names of one or more numeric columns
in \code{data} to summarize using GWSS.}

\item{distance_metric}{Character. One of \code{gw_distance_metric_names()}.
Default: \code{"Euclidean"}.}

\item{kernel}{Character. One of \code{"gaussian"}, \code{"exponential"}, \code{"bisquare"},
\code{"boxcar"}, \code{"tricube"}. Default: \code{"gaussian"}.}

\item{target_crs}{Integer EPSG code used to project county geometries when
\code{longlat = FALSE}. Default: \strong{5070} (NAD83 / CONUS Albers, meters).}

\item{draw_rate}{Numeric in (0, 1]. Fraction of observed counties used during
bandwidth cross-validation. Default: \strong{0.5} (50\%).}

\item{approach}{Character. Bandwidth selection approach passed to
\code{GWmodel::bw.gwr()}. One of \code{"CV"}, \code{"AIC"}, \code{"AICc"}. Default: \code{"CV"}.}

\item{adaptive}{Logical. Use adaptive (nearest-neighbour count) bandwidth
instead of fixed distance. Default: \code{TRUE}.}

\item{state_fips_limits}{Optional character or numeric vector of state FIPS
codes. When supplied, GWSS is evaluated only for counties whose \code{state_fips}
is in this set.}
}
\value{
A \code{data.table} of GW summary statistics for \strong{all counties in the
evaluation set}, with a \code{county_fips} column for merging back to polygons.
Column names follow \strong{GWmodel} conventions for each variable in
\code{variable_list}, e.g. for a variable \code{x}, columns such as \code{x_LM},
\code{x_LSD}, \code{x_LCV}, \code{x_LSKe}, \code{x_LSSke}, etc. are returned (depending on
\code{GWmodel::gwss()} defaults). Attributes attached: \code{"bandwidth"},
\code{"distance_params"}, \code{"kernel"}, \code{"approach"}, \code{"adaptive"}. Returns \code{NULL}
when there are fewer than 5 observed counties.
}
\description{
Computes Geographically Weighted Summary Statistics (GWSS) for one or more
scalar, county-level variables observed in a subset of counties, then
evaluates the statistics at \strong{all} county locations (points-on-surface).
This is useful for spatial smoothing and gap-filling (imputation) when some
counties are missing observations.
}
\details{
The function:
\enumerate{
\item pulls U.S. counties from \strong{urbnmapr} and projects to a suitable CRS;
\item copies \code{data[[fip_col]]} into \code{"county_fips"} and joins to the map;
\item identifies \strong{observed counties} (finite values of the first entry in
\code{variable_list}), fits GWSS using those points, and selects an adaptive
bandwidth by cross-validation on a random subsample sized by \code{draw_rate};
\item optionally restricts the evaluation grid to a subset of states via
\code{state_fips_limits};
\item evaluates GWSS at \strong{all counties in the evaluation set} and returns local
summaries keyed by \code{county_fips}.
}

\strong{Inputs}: \code{data} must contain a county identifier column referenced by
\code{fip_col} and at least one numeric column referenced in \code{variable_list}.
Internally, a column \code{"county_fips"} is created for joining with
\code{urbnmapr::get_urbn_map("counties")}. The data are reduced to a single row
per county (by \code{county_fips}) before fitting.

\strong{Multiple variables}: \code{variable_list} can contain one or more variable
names. Bandwidth selection is performed using the first variable in
\code{variable_list}, but the resulting bandwidth is then used to compute GWSS for
all variables in \code{variable_list} via \code{GWmodel::gwss()}.

\strong{Distance metric} (\code{distance_metric}) defines \verb{(p, theta, longlat)} for
\code{GWmodel::gw.dist()} via \code{resolve_distance_metric()}. Use
\code{gw_distance_metric_names()} to list valid options. If \code{longlat = TRUE}
(e.g., \code{"Great Circle"}), counties are transformed to EPSG:4326; otherwise
they are projected to \code{target_crs} (default 5070, meters).

\strong{Bandwidth selection}: cross-validation (\code{approach = "CV"} by default) is
run on a uniform random subsample of size
\code{ceiling(draw_rate * n_obs)}, bounded to \verb{[5, n_obs - 1]}, where \code{n_obs} is
the number of counties with finite values for the first variable in
\code{variable_list}. Call \code{set.seed()} beforehand for reproducibility. The
function returns \code{NULL} (with a message) if fewer than 5 counties have finite
values.

\strong{State filtering}: if \code{state_fips_limits} is non-\code{NULL}, the evaluation
grid is restricted to counties whose \code{state_fips} is in \code{state_fips_limits}.
This affects both the set of locations at which GWSS is evaluated and the
returned \code{county_fips} set.
}
