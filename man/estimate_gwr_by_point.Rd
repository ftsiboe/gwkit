% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_gwr.R
\name{estimate_gwr_by_point}
\alias{estimate_gwr_by_point}
\title{Geographically weighted local mean/variance regression at points}
\usage{
estimate_gwr_by_point(
  data,
  unit,
  response,
  covariate = "trend",
  coords = c("longitude", "latitude"),
  kernel = "bisquare",
  adaptive = FALSE,
  distance_metric = "Great Circle",
  bw = NULL,
  bw_approach = "AICc",
  bw_sample = 1500L,
  chunk_size = 250L,
  eps = 0.00000001,
  variance = TRUE,
  verbose = FALSE
)
}
\arguments{
\item{data}{A \code{data.table}/data frame with one row per observation, containing
the unit id (\code{unit}), \code{response}, \code{covariate}, and the two coordinate
columns named in \code{coords}. Multiple observations per unit (e.g. a time
series) are expected for a meaningful slope.}

\item{unit}{Character string naming the spatial-unit id column.}

\item{response}{Character string naming the response column.}

\item{covariate}{Character string naming the covariate (e.g. \code{"trend"}).
Default \code{"trend"}.}

\item{coords}{Length-2 character vector naming the longitude/latitude columns.
Default \code{c("longitude", "latitude")}.}

\item{kernel}{GW kernel for \code{GWmodel::bw.gwr()} and the local weights. One of
\code{"gaussian"}, \code{"exponential"}, \code{"bisquare"}, \code{"boxcar"}, \code{"tricube"}.
Default \code{"bisquare"}.}

\item{adaptive}{Logical; adaptive (kNN) bandwidth if \code{TRUE}, fixed distance if
\code{FALSE}. Default \code{FALSE}.}

\item{distance_metric}{One of \code{gw_distance_metric_names()}. Default
\code{"Great Circle"}.}

\item{bw}{Optional pre-computed bandwidth. If \code{NULL} it is selected once via
\code{GWmodel::bw.gwr()} on a random subsample.}

\item{bw_approach}{Bandwidth criterion for \code{bw.gwr()}. Default \code{"AICc"}.}

\item{bw_sample}{Observations sampled for bandwidth selection. Default \code{1500}.}

\item{chunk_size}{Regression points per distance-matrix chunk. Default \code{250}.}

\item{eps}{Added to squared residuals before the log. Default \code{1e-8}.}

\item{variance}{Logical; also estimate the local variance slope. Default
\code{TRUE}.}

\item{verbose}{Logical; print chunk progress. Default \code{FALSE}.}
}
\value{
A \code{data.table} with one row per unit: \code{term}, \code{unit_id},
\code{unit_level}, \code{mean_estimate}, \code{mean_standard_error}, \code{mean_t_value},
\code{mean_p_value}, (when \code{variance}) the \verb{var_*} equivalents, and
\code{model_estimator} (\code{"gwr"}).
}
\description{
For each point-referenced unit, fits a geographically weighted regression of
\code{response} on \code{covariate} (the local slope on the response mean) and, unless
disabled, a second GWR of the log-squared residuals (the local slope on the
response variance). Local slope standard errors, t-values, and normal-
approximation p-values are computed directly (GWmodel returns no SE at
supplied regression points). Distances and the fixed bandwidth use GWmodel.
}
\seealso{
Other Geographically weighted regression: 
\code{\link{estimate_gwr_by_polygon}()},
\code{\link{estimate_gwr_coefficients_by_point}()},
\code{\link{estimate_gwr_coefficients_by_polygon}()}
}
\concept{Geographically weighted regression}
