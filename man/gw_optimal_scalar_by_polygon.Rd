% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gw_consensus_scalar.R
\name{gw_optimal_scalar_by_polygon}
\alias{gw_optimal_scalar_by_polygon}
\title{Consensus scalar for polygon units}
\usage{
gw_optimal_scalar_by_polygon(
  value_dt,
  unit_col,
  polygons,
  value_col = "estimate",
  poly_id = unit_col,
  agg_fun = stats::median,
  probs = c(0.05, 0.95),
  queen_smooth = FALSE,
  include_self = TRUE,
  min_count = 1L,
  max_order = 10L,
  verbose = FALSE
)
}
\arguments{
\item{value_dt}{A \code{data.table}/data frame stacked over settings, containing
the unit id (\code{unit_col}) and the continuous estimate (\code{value_col}).}

\item{unit_col}{Name of the spatial-unit identifier column (e.g.
\code{"polygon_fips"}).}

\item{polygons}{A \code{terra} \code{SpatVector} or \code{sf} polygon layer whose id field
(\code{poly_id}) matches \code{unit_col}.}

\item{value_col}{Name of the continuous value column. Default \code{"estimate"}.}

\item{poly_id}{Name of the id field in \code{polygons}. Default: value of
\code{unit_col}.}

\item{agg_fun, probs, queen_smooth, include_self, min_count, max_order, verbose}{See
\code{gw_optimal_scalar_by_point()}.}
}
\value{
A \code{data.table} with one row per unit (see
\code{gw_optimal_scalar_by_point()}).
}
\description{
As \code{gw_optimal_scalar_by_point()}, but the spatial units are polygons: the
Queen neighbours are shared-boundary contiguities of a supplied geometry
(matching \code{estimate_gwss_by_polygon()}, keyed by e.g. \code{polygon_fips}), and
centroids for the output come from the same geometry. Accepts a \code{terra}
\code{SpatVector} or an \code{sf} object.
}
\seealso{
Other Consensus scalar: 
\code{\link{gw_optimal_scalar_by_point}()}
}
\concept{Consensus scalar}
