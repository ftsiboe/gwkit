% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_gwfe_coefficients.R
\name{estimate_gwfe_coefficients_by_point}
\alias{estimate_gwfe_coefficients_by_point}
\title{Geographically weighted fixed-effects local regression at points}
\usage{
estimate_gwfe_coefficients_by_point(
  data,
  unit,
  formula,
  panel,
  time,
  coords = c("longitude", "latitude"),
  predict_data = NULL,
  distance_metric = "Euclidean",
  kernel = "bisquare",
  adaptive = TRUE,
  bw = NULL,
  bw_approach = "CV",
  terms = NULL,
  fe_df_correction = TRUE
)
}
\arguments{
\item{data}{A \code{data.table}/data frame panel with the model variables, the
\code{panel} id, the \code{time} id, \code{unit}, and the coordinate columns in \code{coords}.}

\item{unit}{Character; the spatial-unit id column (present in source and
target). Usually equal to \code{panel}.}

\item{formula}{A model formula, e.g. \code{lny ~ DD1 + DD2 + DD3 + ppt + ppt2}. The
covariates are demeaned; the response is demeaned via its returned means.}

\item{panel}{Character vector; the fixed-effect panel id column(s).}

\item{time}{Character; the time id column.}

\item{coords}{Length-2 character vector naming longitude/latitude columns.
Default \code{c("longitude", "latitude")}.}

\item{predict_data}{Optional TARGET units (one row per unit) with \code{unit} and
\code{coords}. Defaults to the unique units in \code{data}.}

\item{distance_metric}{One of \code{gw_distance_metric_names()}. Default
\code{"Euclidean"}.}

\item{kernel}{GW kernel. Default \code{"bisquare"}.}

\item{adaptive}{Logical; adaptive (kNN) bandwidth if \code{TRUE}. Default \code{TRUE}.}

\item{bw}{Optional pre-computed bandwidth. If \code{NULL}, selected once via
\code{GWmodel::bw.gwr()} on the demeaned response.}

\item{bw_approach}{Bandwidth criterion. Default \code{"CV"}.}

\item{terms}{Optional character vector restricting the returned terms.}

\item{fe_df_correction}{Logical; rescale the local SE from the lm residual df
to a within df that discounts locally-absorbed unit effects. Default \code{TRUE}.}
}
\value{
A \code{data.table} with one row per (unit x term): \code{term}, \code{unit_id},
\code{unit_level}, \code{est}, \code{se}, \code{tv}, \code{pv}, \code{r_squared}, \code{adj_r_squared},
\code{n_obs}, \code{n_units}, \code{model_estimator} (\code{"gwfe"}). Bandwidth in
\code{attr(., "bandwidth")}; number of panels absorbed in \code{attr(., "NFE")}.
}
\description{
Absorbs panel fixed effects (via \code{\link[=fixed_effect_model_data_prep]{fixed_effect_model_data_prep()}}) and then
estimates the demeaned model by geographically weighted WLS, giving a local
(unit-specific) version of the national fixed-effects coefficients together
with a local within-R^2 for geographically weighted model selection.
}
\seealso{
Other Geographically weighted regression: 
\code{\link{estimate_gwfe_coefficients_by_polygon}()},
\code{\link{estimate_gwr_by_point}()},
\code{\link{estimate_gwr_by_polygon}()},
\code{\link{estimate_gwr_coefficients_by_point}()},
\code{\link{estimate_gwr_coefficients_by_polygon}()}
}
\concept{Geographically weighted regression}
