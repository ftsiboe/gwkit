% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_gwr.R
\name{estimate_gwr}
\alias{estimate_gwr}
\title{Geographically weighted local regression}
\usage{
estimate_gwr(
  data,
  unit,
  formula,
  geometry = NULL,
  coords = c("longitude", "latitude"),
  poly_id = unit,
  predict = NULL,
  panel = NULL,
  time = NULL,
  distance_metric = "Euclidean",
  kernel = "bisquare",
  adaptive = TRUE,
  bw = NULL,
  bw_approach = "CV",
  variance = FALSE,
  fit_stats = FALSE,
  standard_errors = FALSE,
  terms = NULL,
  eps = 1e-08
)
}
\arguments{
\item{data}{A \code{data.table}/data frame with the model variables (and, for
fixed effects, the \code{panel}/\code{time} columns), OR an \code{sf}/\code{SpatVector} carrying
both attributes and geometry (polygon mode).}

\item{unit}{Character; the spatial-unit id column (equal to \code{panel} in
fixed-effects mode).}

\item{formula}{A model formula, e.g. \code{y ~ x} or \code{y ~ x1 + x2}.}

\item{geometry}{Optional \code{sf}/\code{SpatVector} polygon layer (whose \code{poly_id}
matches \code{unit}) supplying per-unit geometry when \code{data} is a plain (e.g.
panel) table. If \code{NULL} and \code{data} is not spatial, point mode is used.}

\item{coords}{Length-2 character vector naming longitude/latitude columns
(point mode). Default \code{c("longitude", "latitude")}.}

\item{poly_id}{Name of the id field in the polygon layer. Default: \code{unit}.}

\item{predict}{Optional target restriction: a data frame of point targets
(point mode) or a character id vector / data frame with \code{unit} (polygon
mode). Defaults to the unique units in \code{data}.}

\item{panel, time}{Optional fixed-effects panel id and time id. If \code{panel} is
supplied the panel fixed effects are absorbed (within transform) before the
GW fit and \code{model_estimator} is \code{"gwfe"}; otherwise \code{"gwr"}.}

\item{distance_metric}{One of \code{gw_distance_metric_names()}. Default
\code{"Euclidean"}.}

\item{kernel}{GW kernel. Default \code{"bisquare"}.}

\item{adaptive}{Logical; adaptive (kNN) bandwidth if \code{TRUE}. Default \code{TRUE}.}

\item{bw}{Optional pre-computed bandwidth. If \code{NULL}, selected once via
\code{GWmodel::bw.gwr()} on the (demeaned) response, intercept-only (\code{. ~ 1}):
the bandwidth targets the response surface, not the full covariate model.
Model columns may be named anything (including \code{X}/\code{Y}) - internal
coordinate columns are dot-prefixed and will not collide.}

\item{bw_approach}{Bandwidth criterion. Default \code{"CV"}.}

\item{variance}{Logical; also fit the local slope of the log-squared residuals
(a heteroskedasticity companion), returned as \code{estimand == "variance"} rows.
Requires the targets to cover the source units. Default \code{FALSE}.}

\item{fit_stats}{Logical; attach local within-\code{r_squared} and \code{n_obs}.
Default \code{FALSE}.}

\item{standard_errors}{Logical; also return analytic WLS \code{se}/\code{tv}/\code{pv}.
Default \code{FALSE} - coefficients only (identical to \code{lm}); use the bootstrap.}

\item{terms}{Optional character vector restricting the returned terms.}

\item{eps}{Added to squared residuals before the log (variance mode). Default
\code{1e-8}.}
}
\value{
A \code{data.table}, one row per \code{unit_id} x \code{term} x \code{estimand}
(\code{estimand} in \code{"mean"}/\code{"variance"}): \code{unit_id}, \code{term}, \code{estimand},
\code{unit_level}, \code{estimate}, \code{se}, \code{tv}, \code{pv}, (when \code{fit_stats}) \code{r_squared},
\code{n_obs}, and \code{model_estimator}. Bandwidth in \code{attr(., "bandwidth")}; number
of panels absorbed (FE mode) in \code{attr(., "NFE")}.
}
\description{
One geographically weighted regression covering plain GWR, fixed-effects GWR,
and the mean/variance decomposition, over the shared local WLS engine. Point
vs polygon mode is detected by class: pass an \code{sf}/\code{terra} \code{SpatVector} (as
\code{data}, or via \code{geometry} for panel inputs) for polygon units - each reduced
to its point-on-surface; otherwise the \code{coords} columns of \code{data} are used.
}
\seealso{
Other Geographically weighted regression: 
\code{\link{estimate_gwr_kernels}()}
}
\concept{Geographically weighted regression}
