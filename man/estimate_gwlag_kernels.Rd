% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_gwlag.R
\name{estimate_gwlag_kernels}
\alias{estimate_gwlag_kernels}
\title{Geographically weighted spatial lag across kernels (one distance metric)}
\usage{
estimate_gwlag_kernels(
  data,
  unit,
  value_cols,
  geometry = NULL,
  coords = c("longitude", "latitude"),
  poly_id = unit,
  predict = NULL,
  distance_metric = "Euclidean",
  kernel = c("gaussian", "exponential", "bisquare", "boxcar", "tricube"),
  adaptive = FALSE,
  bw = NULL,
  bandwidth = c("shared", "per_kernel"),
  bw_response = NULL,
  bw_approach = "CV",
  include_self = FALSE
)
}
\arguments{
\item{data}{A \code{data.table}/data frame of the SOURCE units (one row per unit)
with \code{unit}, all \code{value_cols}, and - in point mode - the \code{coords} columns;
or an \code{sf}/\code{SpatVector} carrying both attributes and geometry (polygon mode).}

\item{unit}{Character; the unit-id column.}

\item{value_cols}{Character vector of numeric columns to lag. Each returns a
column named \verb{<value_col>_LM}.}

\item{geometry}{Optional \code{sf}/\code{SpatVector} polygon layer (id field \code{poly_id}
matching \code{unit}) supplying per-unit geometry. If \code{NULL} and \code{data} is not
spatial, point mode uses \code{coords}.}

\item{coords}{Length-2 character vector naming longitude/latitude columns
(point mode). Default \code{c("longitude", "latitude")}.}

\item{poly_id}{Name of the id field in the polygon layer. Default: \code{unit}.}

\item{predict}{Optional target restriction: a data frame of point targets
(point mode) or a character id vector / data frame with \code{unit} (polygon
mode). Defaults to the units in \code{data}.}

\item{distance_metric}{One of \code{gw_distance_metric_names()}. Default
\code{"Euclidean"}.}

\item{kernel}{Character vector of kernels to evaluate (any of \code{"gaussian"},
\code{"exponential"}, \code{"bisquare"}, \code{"boxcar"}, \code{"tricube"}). Default: all five.}

\item{adaptive}{Logical; adaptive (kNN) bandwidth if \code{TRUE}. Default \code{FALSE}
(fixed-distance); note \code{estimate_gwr()}/\code{estimate_gwss()} default to \code{TRUE}.}

\item{bw}{Optional bandwidth override: a single numeric (used for every kernel)
or a \strong{named} numeric keyed by kernel. When supplied no selection is done.}

\item{bandwidth}{Bandwidth strategy when \code{bw} is \code{NULL}: \code{"shared"} (default)
selects one bandwidth (on the first kernel) and reuses it across kernels -
one \code{GWmodel::bw.gwr()} call instead of \code{length(kernel)}; \code{"per_kernel"}
selects a bandwidth per kernel (matches looping \code{estimate_gwlag()} exactly).}

\item{bw_response}{Column used for bandwidth selection when \code{bw} is \code{NULL}.
Default: the first \code{value_cols}.}

\item{bw_approach}{Bandwidth criterion. Default \code{"CV"}.}

\item{include_self}{Logical; keep the focal unit in its own neighbourhood.
Default \code{FALSE} (strict neighbour mean).}
}
\value{
A \code{data.table} stacked over \code{kernel} (a \code{kernel} column added to the
\code{estimate_gwlag()} layout: the \code{unit} id and one \verb{<value_col>_LM} column per
value column). Per-kernel bandwidths are attached as a named numeric in
\code{attr(., "bandwidth")}.
}
\description{
Multi-kernel companion to \code{estimate_gwlag()}: computes the neighbour-weighted
spatial lag under several kernels for a single \code{distance_metric}, building the
(kernel-independent) obs->target distance matrix and the bandwidth only \strong{once}
and reusing them across kernels. It is the availability-side analogue of
\code{estimate_gwr_kernels()} / \code{estimate_gwss_kernels()} - much cheaper than calling
\code{estimate_gwlag()} once per kernel over a spec ensemble.
}
\seealso{
\code{estimate_gwlag()}, \code{estimate_gwr_kernels()}

Other Geographically weighted summaries: 
\code{\link{estimate_gwlag}()},
\code{\link{estimate_gwss}()},
\code{\link{estimate_gwss_kernels}()},
\code{\link{gw_select_bandwidth}()}
}
\concept{Geographically weighted summaries}
