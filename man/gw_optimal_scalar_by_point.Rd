% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gw_consensus_scalar.R
\name{gw_optimal_scalar_by_point}
\alias{gw_optimal_scalar_by_point}
\title{Consensus scalar for point (lattice) units}
\usage{
gw_optimal_scalar_by_point(
  value_dt,
  unit_col,
  value_col = "estimate",
  coords = c("longitude", "latitude"),
  agg_fun = stats::median,
  probs = c(0.05, 0.95),
  queen_smooth = FALSE,
  include_self = TRUE,
  min_count = 1L,
  max_order = 10L,
  verbose = FALSE
)
}
\arguments{
\item{value_dt}{A \code{data.table}/data frame stacked over settings, containing
the unit id (\code{unit_col}), the continuous estimate (\code{value_col}), and the two
coordinate columns named in \code{coords}.}

\item{unit_col}{Name of the spatial-unit identifier column (e.g. \code{"grid_id"}).}

\item{value_col}{Name of the continuous value column. Default \code{"estimate"}.}

\item{coords}{Length-2 character vector naming the longitude/latitude columns.
Default \code{c("longitude", "latitude")}.}

\item{agg_fun}{Function reducing a numeric vector (the values across settings
for one unit) to a single scalar. Default \code{stats::median}. Any function of
the form \code{function(x) ...} works, e.g. \code{mean}, \code{function(x) mean(x, trim = 0.1)}.}

\item{probs}{Length-2 numeric; the lower/upper quantiles reported across
settings as \code{consensus_lo}/\code{consensus_hi}. Default \code{c(0.05, 0.95)}.}

\item{queen_smooth}{Logical; if \code{TRUE}, also compute the Queen-neighbourhood
smoothed consensus. Default \code{FALSE}.}

\item{include_self}{Logical; include the unit itself in the Queen
neighbourhood. Default \code{TRUE}.}

\item{min_count}{Minimum number of units to gather before reducing the Queen
neighbourhood; the order widens until reached. Default \code{1}.}

\item{max_order}{Maximum Queen order to expand to. Default \code{10}.}

\item{verbose}{Logical; print progress. Default \code{FALSE}.}
}
\value{
A \code{data.table} with one row per unit: the id column, \code{longitude},
\code{latitude}, \code{n_settings}, \code{consensus}, \code{consensus_sd}, \code{consensus_mad},
\code{consensus_lo}, \code{consensus_hi}, \code{sign_agreement}, and - when
\code{queen_smooth = TRUE} - \code{queen_value}, \code{queen_order}, \code{queen_agreement}.
}
\description{
Collapses a stacked table of per-unit continuous estimates (one row per unit
per \code{distance_metric} x \code{kernel} setting, within a history window) to a single
per-unit consensus value via a user-supplied reducer (\code{agg_fun}, default the
median), together with the spec-uncertainty spread and a sign-agreement share.
Optionally reconciles each unit's consensus with its first-order Queen
neighbourhood, widening the Queen order until enough neighbours are gathered.
Coordinates are read directly from the table, matching \code{estimate_gwss_by_point()}.
}
\seealso{
Other Consensus scalar: 
\code{\link{gw_optimal_scalar_by_polygon}()}
}
\concept{Consensus scalar}
