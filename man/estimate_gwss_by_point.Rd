% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_gwss.R
\name{estimate_gwss_by_point}
\alias{estimate_gwss_by_point}
\title{Estimate geographically weighted summary statistics (GWSS) for points}
\usage{
estimate_gwss_by_point(
  locat_observed,
  locat_predict,
  longitude_col,
  latitude_col,
  variable,
  distance_metric = "Euclidean",
  kernel = "gaussian",
  target_crs = 5070,
  draw_rate = 0.5,
  approach = "CV",
  adaptive = TRUE,
  feasible_radius = 100,
  group_variable = NULL,
  identifiers = NULL
)
}
\arguments{
\item{locat_observed}{\code{data.frame}/\code{data.table} with columns named by
\code{longitude_col}, \code{latitude_col}, and a numeric column named by \code{variable}.
Must be in decimal degrees (EPSG:4326).}

\item{locat_predict}{\code{data.frame}/\code{data.table} with columns named by
\code{longitude_col} and \code{latitude_col}. Must be in decimal degrees (EPSG:4326).
Rows with no nearby observations (within \code{feasible_radius}) are dropped.}

\item{longitude_col, latitude_col}{\code{character} column names for coordinates.}

\item{variable}{\code{character} name of numeric column in \code{locat_observed} to summarize.}

\item{distance_metric}{\code{character} distance metric passed to
\code{GWmodel::gw.dist()} via \code{resolve_distance_metric()}. Default \code{"Euclidean"}.}

\item{kernel}{\code{character} kernel for GWR/GWSS weights. One of
\code{"gaussian"}, \code{"exponential"}, \code{"bisquare"}, \code{"boxcar"}, \code{"tricube"}.
Default \code{"gaussian"}.}

\item{target_crs}{\code{integer} EPSG code used for projection during radius
pre-screening (in meters). Default \code{5070} (NAD83 / CONUS Albers).}

\item{draw_rate}{\code{numeric} fraction (0,1] of observed points used for
bandwidth selection. Default \code{0.5}.}

\item{approach}{\code{character} one of \code{"CV"}, \code{"AIC"}, \code{"AICc"}, passed to
\code{GWmodel::bw.gwr()}. Default \code{"CV"}.}

\item{adaptive}{\code{logical} whether to use adaptive (kNN) bandwidth. Default \code{TRUE}.}

\item{feasible_radius}{\code{numeric} radius in \strong{miles} for pre-screening prediction
locations. Default \code{100}.}

\item{group_variable}{\code{character} or \code{NULL}. Optional column in \code{locat_predict}
used to partition prediction points; observed points are always global.}

\item{identifiers}{\code{character} vector of column names from \code{locat_predict} to
carry through to the output.}
}
\value{
A \code{data.table} containing:
\itemize{
\item GWSS summary statistics (e.g., local mean, SD, etc.)
\item \code{longitude}, \code{latitude} - coordinates of prediction points
\item \code{block_variable} - group label or 1L
\item \code{bandwidth} - estimated global bandwidth
\item \code{p}, \code{theta}, \code{longlat}, \code{approach}, \code{adaptive}, \code{variable} - metadata
\item any columns from \code{identifiers}
}
Returns an empty \code{data.table} if no prediction locations pass the radius screen.
}
\description{
Computes geographically weighted summary statistics (via \strong{GWmodel::gwss}) at
specified prediction locations using a \strong{single global bandwidth} determined
from a subsample of observed points. All observed points are used for every
prediction; \code{group_variable} only partitions the prediction set for
organizational or computational reasons.
}
\details{
\itemize{
\item Coordinates are assumed to be in \strong{WGS84 (EPSG:4326)}.
\item Prediction points without any observed points within \code{feasible_radius}
miles are excluded.
\item Bandwidth is estimated \strong{once} via \code{GWmodel::bw.gwr()} on a random subsample
(size = \code{draw_rate * n_obs}, bounded to 5, n_obs - 1).
\item \code{group_variable}, if provided, splits the prediction set for parallel or
modular computation, but the same global bandwidth and all observed points
are used for each group.
}
}
\section{Implementation notes}{

\itemize{
\item Coordinates must be valid WGS84 (longitude = x, latitude = y).
\item Radius pre-screen is computed using projected distances (in meters).
\item Global bandwidth ensures comparability across groups.
\item Random seed (\code{set.seed(1L)}) ensures reproducibility of CV subsampling.
}
}

\references{
Fotheringham, A. S., Brunsdon, C., & Charlton, M. (2002).
\emph{Geographically Weighted Regression: The Analysis of Spatially Varying Relationships.}
John Wiley & Sons.
}
