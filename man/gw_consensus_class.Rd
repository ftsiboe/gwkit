% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gw_consensus.R
\name{gw_consensus_class}
\alias{gw_consensus_class}
\title{Consensus class across the kernel x distance-metric domain}
\usage{
gw_consensus_class(
  class_dt,
  unit_col,
  geometry = NULL,
  class_col = "trend_class_05",
  coords = c("longitude", "latitude"),
  poly_id = unit_col,
  include_self = TRUE,
  max_order = 10L,
  class_levels = NULL,
  verbose = FALSE
)
}
\arguments{
\item{class_dt}{A \code{data.table}/data frame stacked over settings, containing
the unit id (\code{unit_col}), the categorical class (\code{class_col}), and - in
point mode - the coordinate columns named in \code{coords}.}

\item{unit_col}{Name of the spatial-unit identifier column (e.g. \code{"grid_id"}).}

\item{geometry}{Optional \code{sf}/\code{SpatVector} polygon layer (id field \code{poly_id}
matching \code{unit_col}) for polygon mode. If \code{NULL}, point mode uses \code{coords}.}

\item{class_col}{Name of the categorical class column. Default
\code{"trend_class_05"}.}

\item{coords}{Length-2 character vector naming the longitude/latitude columns
(point mode). Default \code{c("longitude", "latitude")}.}

\item{poly_id}{Name of the id field in \code{geometry}. Default: \code{unit_col}.}

\item{include_self}{Logical; count the unit itself in the Queen vote. Default
\code{TRUE}.}

\item{max_order}{Maximum Queen order to expand to when breaking ties. Default
\code{10}.}

\item{class_levels}{Optional character vector of all class labels (fixes the
count columns / vote ordering). Default: sorted unique observed classes.}

\item{verbose}{Logical; print progress. Default \code{FALSE}.}
}
\value{
A \code{data.table} with one row per unit: the id column, \code{longitude},
\code{latitude}, \code{n_settings}, \code{modal_class}, \code{modal_agreement}, \code{queen_class},
\code{queen_order} (order at which the vote resolved), and \code{queen_agreement}.
}
\description{
Collapses a stacked table of per-unit classifications (one row per unit per
\code{distance_metric} x \code{kernel} setting, within a history window) to a single
consensus class per unit: the mode across settings (\code{modal_class}) plus a
first-order Queen contiguity vote for optional spatial de-speckling
(\code{queen_class}), with ties resolved by widening the Queen order. Point vs
polygon mode is class-detected: pass an \code{sf}/\code{SpatVector} \code{geometry} for
polygon units (Queen = shared boundaries); otherwise \code{coords} columns are read
from the table (Queen = lattice).
}
\seealso{
Other Consensus: 
\code{\link{gw_consensus_scalar}()}
}
\concept{Consensus}
