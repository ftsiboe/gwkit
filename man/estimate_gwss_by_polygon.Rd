% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_gwss.R
\name{estimate_gwss_by_polygon}
\alias{estimate_gwss_by_polygon}
\title{Estimate geographically weighted summary statistics for polygons}
\usage{
estimate_gwss_by_polygon(
  data,
  shape_file,
  fip_col,
  variable_list,
  distance_metric = "Euclidean",
  kernel = "gaussian",
  target_crs = 5070,
  draw_rate = 0.5,
  approach = "CV",
  adaptive = TRUE
)
}
\arguments{
\item{data}{A \code{data.frame} or \code{data.table} containing the polygon identifier
column specified by \code{fip_col} and all numeric columns listed in
\code{variable_list}.}

\item{shape_file}{An \code{sf} polygon object containing the polygon geometries.
This object must contain the polygon identifier column specified by
\code{fip_col}.}

\item{fip_col}{Character. Name of the polygon identifier column shared by
\code{data} and \code{shape_file}. This column is copied internally to
\code{polygon_fips} and used for joining the observed data to the polygon layer.}

\item{variable_list}{Character vector. Names of one or more numeric columns
in \code{data} to summarize using GWSS. The first variable in this vector is
also used to define the observed polygons and to select the bandwidth.}

\item{distance_metric}{Character. Name of the distance metric to use. Must be
one of the values supported by \code{gw_distance_metric_names()}. The selected
metric is resolved by \code{resolve_distance_metric()} into the \code{p}, \code{theta},
and \code{longlat} arguments used by \code{GWmodel}.}

\item{kernel}{Character. Kernel function passed to \code{GWmodel::bw.gwr()} and
\code{GWmodel::gwss()}. Must be one of \code{"gaussian"}, \code{"exponential"},
\code{"bisquare"}, \code{"boxcar"}, or \code{"tricube"}. Default is \code{"gaussian"}.}

\item{target_crs}{Integer EPSG code used to project polygon geometries when
the selected distance metric does not use longitude/latitude distances.
Default is \code{5070}, NAD83 / CONUS Albers.}

\item{draw_rate}{Numeric value in \verb{(0, 1]}. Fraction of observed polygons
used for bandwidth cross-validation. The actual subsample size is bounded
between 5 and \code{n_obs - 1}, where \code{n_obs} is the number of observed polygons.
Default is \code{0.5}.}

\item{approach}{Character. Bandwidth selection criterion passed to
\code{GWmodel::bw.gwr()}. Must be one of \code{"CV"}, \code{"AIC"}, or \code{"AICc"}.
Default is \code{"CV"}.}

\item{adaptive}{Logical. If \code{TRUE}, use an adaptive nearest-neighbor
bandwidth. If \code{FALSE}, use a fixed-distance bandwidth. Default is \code{TRUE}.}
}
\value{
A \code{data.table} containing geographically weighted summary statistics
evaluated at all polygons in \code{shape_file}. The returned table includes
the polygon identifier column and GWSS output columns following
\code{GWmodel::gwss()} naming conventions. For a variable named \code{x}, returned
columns may include local statistics such as \code{x_LM}, \code{x_LSD}, \code{x_LCV},
\code{x_LSKe}, and related GWSS summaries, depending on \code{GWmodel::gwss()}
defaults.

The returned object also has the following attributes:
\describe{
\item{bandwidth}{The selected GW bandwidth.}
\item{distance_params}{A list containing \code{p}, \code{theta}, and \code{longlat}.}
\item{kernel}{The kernel used for bandwidth selection and GWSS.}
\item{approach}{The bandwidth selection approach.}
\item{adaptive}{Whether an adaptive bandwidth was used.}
}
}
\description{
This function estimates Geographically Weighted Summary Statistics (GWSS)
for one or more numeric variables observed for a subset of polygons, then
evaluates the local summary statistics at all polygon locations in a supplied
spatial polygon layer. It is useful for spatial smoothing and gap-filling
when some polygons do not have observed values.
}
\details{
The function first prepares the input data by copying the polygon identifier
specified by \code{fip_col} into a common internal column named \code{polygon_fips}.
It then keeps one finite observation per polygon, joins those observations
to the supplied \code{shape_file}, and uses the observed polygons to fit GWSS.
The estimated geographically weighted summaries are then evaluated at all
polygon locations using points-on-surface.

The function is designed for polygon-level spatial smoothing. Observed values
are taken from \code{data}, while the full set of possible evaluation locations
comes from \code{shape_file}. This means the returned object can include polygons
that were not present in \code{data}, with their local summaries estimated from
nearby observed polygons.

Internally, the function:
\enumerate{
\item validates the input data, variables, kernel, and bandwidth-selection
approach;
\item resolves the selected distance metric using
\code{resolve_distance_metric()};
\item creates a generic polygon identifier, \code{polygon_fips}, from \code{fip_col};
\item keeps one finite observation per polygon based on the first variable
in \code{variable_list};
\item transforms the polygon layer to EPSG:4326 when \code{longlat = TRUE}, or
to \code{target_crs} otherwise;
\item joins the observed data to the polygon geometries while retaining all
polygons in \code{shape_file};
\item selects a bandwidth using \code{GWmodel::bw.gwr()} on a random subsample of
observed polygons;
\item computes GWSS using observed polygons as the data locations and all
polygons in \code{shape_file} as the summary locations.
}

Bandwidth selection uses only the first variable in \code{variable_list}, but the
selected bandwidth is then used to compute GWSS for all variables in
\code{variable_list}. Use \code{set.seed()} before calling this function if
reproducible subsampling is needed.

The function returns \code{NULL} with a message when fewer than five polygons have
finite observed values for the first variable in \code{variable_list}.
}
