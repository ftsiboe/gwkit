% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_gwr.R
\name{estimate_gwr_kernels}
\alias{estimate_gwr_kernels}
\title{Geographically weighted regression across kernels (one distance metric)}
\usage{
estimate_gwr_kernels(
  data,
  unit,
  formula,
  geometry = NULL,
  coords = c("longitude", "latitude"),
  poly_id = unit,
  predict = NULL,
  panel = NULL,
  time = NULL,
  distance_metric = "Euclidean",
  kernel = c("gaussian", "exponential", "bisquare", "boxcar", "tricube"),
  adaptive = TRUE,
  bw = NULL,
  bandwidth = c("shared", "per_kernel"),
  bw_approach = "CV",
  fit_stats = FALSE,
  standard_errors = FALSE,
  terms = NULL
)
}
\arguments{
\item{data}{A \code{data.table}/data frame with the model variables (and, for
fixed effects, the \code{panel}/\code{time} columns), OR an \code{sf}/\code{SpatVector} carrying
both attributes and geometry (polygon mode).}

\item{unit}{Character; the spatial-unit id column (equal to \code{panel} in
fixed-effects mode).}

\item{formula}{A model formula, e.g. \code{y ~ x} or \code{y ~ x1 + x2}.}

\item{geometry}{Optional \code{sf}/\code{SpatVector} polygon layer (whose \code{poly_id}
matches \code{unit}) supplying per-unit geometry when \code{data} is a plain (e.g.
panel) table. If \code{NULL} and \code{data} is not spatial, point mode is used.}

\item{coords}{Length-2 character vector naming longitude/latitude columns
(point mode). Default \code{c("longitude", "latitude")}.}

\item{poly_id}{Name of the id field in the polygon layer. Default: \code{unit}.}

\item{predict}{Optional target restriction: a data frame of point targets
(point mode) or a character id vector / data frame with \code{unit} (polygon
mode). Defaults to the unique units in \code{data}.}

\item{panel, time}{Optional fixed-effects panel id and time id. If \code{panel} is
supplied the panel fixed effects are absorbed (within transform) before the
GW fit and \code{model_estimator} is \code{"gwfe"}; otherwise \code{"gwr"}.}

\item{distance_metric}{One of \code{gw_distance_metric_names()}. Default
\code{"Euclidean"}.}

\item{kernel}{Character vector of kernels to evaluate (any of \code{"gaussian"},
\code{"exponential"}, \code{"bisquare"}, \code{"boxcar"}, \code{"tricube"}). Default: all five.}

\item{adaptive}{Logical; adaptive (kNN) bandwidth if \code{TRUE}. Default \code{TRUE}.}

\item{bw}{Optional pre-computed bandwidth override. Either a single numeric
(used for every kernel) or a \strong{named} numeric keyed by kernel (per-kernel
reuse across calls, e.g. across a grid of models). When supplied no
bandwidth selection is performed. Because the bandwidth is chosen on the
(FE-demeaned) response intercept-only, it is invariant to the covariates, so
the selected value returned in \code{attr(., "bandwidth")} can be fed back as
\code{bw} for subsequent fits that differ only in their covariates.}

\item{bandwidth}{Bandwidth strategy when \code{bw} is \code{NULL}: \code{"shared"} (default)
selects one bandwidth (on the first kernel) and reuses it for all kernels -
one \code{GWmodel::bw.gwr()} call instead of \code{length(kernel)}; \code{"per_kernel"}
selects a bandwidth per kernel (matches looping \code{estimate_gwr()} exactly).}

\item{bw_approach}{Bandwidth criterion. Default \code{"CV"}.}

\item{fit_stats}{Logical; attach local within-\code{r_squared} and \code{n_obs}.
Default \code{FALSE}.}

\item{standard_errors}{Logical; also return analytic WLS \code{se}/\code{tv}/\code{pv}.
Default \code{FALSE} - coefficients only (identical to \code{lm}); use the bootstrap.}

\item{terms}{Optional character vector restricting the returned terms.}
}
\value{
A \code{data.table} stacked over \code{kernel} (a \code{kernel} column added to the
\code{estimate_gwr()} long layout: one row per \code{unit_id} x \code{term} x \code{estimand} x
\code{kernel}). The per-kernel bandwidths are attached as a named numeric in
\code{attr(., "bandwidth")}; number of panels absorbed (FE mode) in
\code{attr(., "NFE")}. \code{estimand} is always \code{"mean"} (variance mode unsupported).
}
\description{
Multi-kernel companion to \code{estimate_gwr()}: fits the same model under several
kernels for a single \code{distance_metric}, computing the (kernel-independent)
obs->target distance matrix and the bandwidth only \strong{once} and reusing them
across kernels. It is the regression analogue of \code{estimate_gwss_kernels()} and
the per-metric building block for spec ensembles (10 metrics x 5 kernels): far
cheaper than calling \code{estimate_gwr()} once per kernel. Point vs polygon mode,
fixed-effects absorption (\code{panel}/\code{time}), \code{fit_stats}, opt-in
\code{standard_errors}, and \code{terms} behave exactly as in \code{estimate_gwr()}.
}
\seealso{
\code{estimate_gwr()}, \code{estimate_gwss_kernels()}

Other Geographically weighted regression: 
\code{\link{estimate_gwr}()}
}
\concept{Geographically weighted regression}
