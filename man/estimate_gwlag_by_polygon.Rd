% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_gwlag.R
\name{estimate_gwlag_by_polygon}
\alias{estimate_gwlag_by_polygon}
\title{Geographically weighted spatial lag on polygons}
\usage{
estimate_gwlag_by_polygon(
  data,
  unit,
  polygons,
  value_cols,
  poly_id = unit,
  predict_ids = NULL,
  distance_metric = "Euclidean",
  kernel = "bisquare",
  adaptive = FALSE,
  bw = NULL,
  bw_response = NULL,
  bw_approach = "CV",
  include_self = FALSE
)
}
\arguments{
\item{data}{Source units (one row per unit) with \code{unit} and all \code{value_cols}.}

\item{unit}{Character; the unit-id column.}

\item{polygons}{A \code{terra} \code{SpatVector} or \code{sf} polygon layer whose id field
(\code{poly_id}) matches \code{unit}. Provides target ids and centroids; the value
columns are joined from \code{data} (and \code{predict_ids} restricts the targets).}

\item{value_cols}{Character vector of numeric columns to lag.}

\item{poly_id}{Name of the id field in \code{polygons}. Default: value of \code{unit}.}

\item{predict_ids}{Optional character vector of target unit ids (subset of the
polygon ids). Default: all polygon units.}

\item{distance_metric, kernel, adaptive, bw, bw_response, bw_approach, include_self}{See \code{estimate_gwlag_by_point()}.}
}
\value{
A \code{data.table} with one row per target unit (see
\code{estimate_gwlag_by_point()}).
}
\description{
As \code{estimate_gwlag_by_point()}, but the spatial units are polygons; centroid
coordinates are derived from \code{polygons} and joined to \code{data} (and, if given,
\code{predict_data}) by the unit id.
}
\seealso{
Other Geographically weighted summaries: 
\code{\link{estimate_gwlag_by_point}()}
}
\concept{Geographically weighted summaries}
