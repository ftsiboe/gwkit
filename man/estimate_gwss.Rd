% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_gwss.R
\name{estimate_gwss}
\alias{estimate_gwss}
\title{Estimate geographically weighted summary statistics (GWSS)}
\usage{
estimate_gwss(
  data,
  variable_list,
  geometry = NULL,
  us_counties = FALSE,
  id_col = NULL,
  coords = c("longitude", "latitude"),
  predict = NULL,
  distance_metric = "Euclidean",
  kernel = "gaussian",
  adaptive = TRUE,
  approach = "CV",
  target_crs = 5070,
  draw_rate = 0.5,
  feasible_radius = 100,
  identifiers = NULL,
  state_fips_limits = NULL
)
}
\arguments{
\item{data}{A \code{data.table}/data frame with \code{variable_list} and either the
\code{coords} columns (point mode) or the \code{id_col} join key (polygon mode).}

\item{variable_list}{Character vector of numeric variable column(s) to
summarize (passed to \code{GWmodel::gwss(vars = )}). The bandwidth uses the first.}

\item{geometry}{Optional \code{sf}/\code{SpatVector} polygon layer whose \code{id_col} field
matches \code{data[[id_col]]} (polygon mode). If \code{NULL} and \code{us_counties = FALSE},
point mode is used.}

\item{us_counties}{Logical; if \code{TRUE}, pull the U.S. county polygons from
\code{urbnmapr} (polygon mode). \code{id_col} defaults to \code{"county_fips"}.}

\item{id_col}{Character; the unit id column shared by \code{data} and \code{geometry}
(polygon mode). Required in polygon mode.}

\item{coords}{Length-2 character vector naming longitude/latitude columns
(point mode). Default \code{c("longitude", "latitude")}.}

\item{predict}{Optional prediction locations (point mode): a data frame with
the \code{coords} columns (and any \code{identifiers}). Defaults to the observed points.}

\item{distance_metric}{One of \code{gw_distance_metric_names()}. Default
\code{"Euclidean"}.}

\item{kernel}{GW kernel. Default \code{"gaussian"}.}

\item{adaptive}{Logical; adaptive (kNN) bandwidth if \code{TRUE}. Default \code{TRUE}.}

\item{approach}{Bandwidth criterion, one of \code{"CV"}, \code{"AIC"}, \code{"AICc"}.
Default \code{"CV"}.}

\item{target_crs}{Integer EPSG code (meters) for projection (polygon geometry
and the point-mode radius screen). Default \code{5070} (NAD83 / CONUS Albers).}

\item{draw_rate}{Fraction of observed units used for bandwidth selection.
Default \code{0.5}.}

\item{feasible_radius}{Radius in miles for pre-screening prediction points
(point mode only). Default \code{100}.}

\item{identifiers}{Optional character vector of columns in \code{predict} (point
mode) to carry through to the output.}

\item{state_fips_limits}{Optional state FIPS to restrict the county set
(\code{us_counties}/polygon mode, when a \code{state_fips} column is present).}
}
\value{
A \code{data.table} of GWSS statistics with one row per prediction unit,
the unit id (polygon mode) or coordinates + \code{identifiers} (point mode). The
bandwidth and distance parameters are attached as attributes.
}
\description{
One entry point over \code{GWmodel::gwss()}: at each prediction location it returns
local summary statistics (local mean, SD, CV, skewness, and local covariance /
correlation between variables). Point vs polygon mode is detected by class.
}
\seealso{
Other Geographically weighted summaries: 
\code{\link{estimate_gwlag}()},
\code{\link{estimate_gwlag_kernels}()},
\code{\link{estimate_gwss_kernels}()},
\code{\link{gw_select_bandwidth}()}
}
\concept{Geographically weighted summaries}
